// File: src/utils/virtualizedListHelpers.js

// Helper function to get an item from data at a given index
export const getItem = (data, index) => data[index];

// Helper function to get the total count of items in data
export const getItemCount = (data) => data.length;
